import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Button from 'topo/Atom/Button';

<Meta title="Atom/Button" />

# Button

When you want something to be clickable, use a button! It takes the following props:

- `variant` -- One of `"solid"` (default), `"outline"`, or `"ghost"`
- `colorScheme` -- Any brand color, without the shade (e.g. `green`, but not `green.500`). Default is gray.
- `size` -- One of `"xs"`, `"sm"`, `"md"` (default), `"lg"`.

## Variants

<Canvas>
  <Story name="Button Variants">
    <>
      <Button variant="solid" colorScheme="brand">solid</Button>{' '}
      <Button variant="outline" colorScheme="brand">outline</Button>{' '}
      <Button variant="ghost" colorScheme="brand">ghost</Button>{' '}
    </>
  </Story>
</Canvas>

## Colors

<Canvas>
  <Story name="Button Colors">
    <>
      <Button variant="solid" colorScheme="gray">gray</Button>{' '}
      <Button variant="solid" colorScheme="red">red</Button>{' '}
      <Button variant="solid" colorScheme="orange">orange</Button>{' '}
      <Button variant="solid" colorScheme="yellow">yellow</Button>{' '}
      <Button variant="solid" colorScheme="green">green</Button>{' '}
      <Button variant="solid" colorScheme="teal">teal</Button>{' '}
      <Button variant="solid" colorScheme="cyan">cyan</Button>{' '}
      <Button variant="solid" colorScheme="blue">blue</Button>{' '}
      <Button variant="solid" colorScheme="indigo">indigo</Button>{' '}
      <Button variant="solid" colorScheme="purple">purple</Button>{' '}
      <Button variant="solid" colorScheme="pink">pink</Button>{' '}
    </>
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story name="Button Sizes">
    <>
      <Button size="xs">xs</Button>{' '}
      <Button size="sm">sm</Button>{' '}
      <Button size="md">md</Button>{' '}
      <Button size="lg">lg</Button>{' '}
    </>
  </Story>
</Canvas>
