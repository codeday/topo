import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Spinner from 'topo/Atom/Spinner';

<Meta title="Atom/Spinner" component={Spinner} />

# Spinner

<Canvas>
  <Story name="Spinner">
    <Spinner />
  </Story>
</Canvas>

Were you waiting for that to load? That's because this is our spinner!

The color currently cannot be customized, because not all browsers support SVG animations, so we have to use
GIF as a fallback. Soon.
