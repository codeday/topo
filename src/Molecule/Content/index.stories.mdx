import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Header, { SiteLogo, Menu } from 'topo/Organism/Header';
import Button from 'topo/Atom/Button';
import { Clear } from 'topo/Atom/Logo';
import Content from 'topo/Molecule/Content';

<Meta title="Molecule/Content" />

# Content

Most of your content should be in a `Content` box, which sets a standard width and `margin: 0 auto`. Y(ou can set the
`wide` or `full` props to make the content wider.)

<Canvas>
  <Story name="Divider">
    <>
      <Header underscore>
        <SiteLogo>
          <Clear withText />
        </SiteLogo>
        <Menu>
          <Button href="#">Hi</Button>
        </Menu>
      </Header>
      <Content>
        <Text>
          This won't look like much in the docs view, but switch to the Canvas tab and you should see it's centered.
        </Text>
      </Content>
      <Content wide>
          This is a wider box, for emphasis.
      </Content>
    </>
  </Story>
</Canvas>
